# Secrets
.env

# Throwaway: download cache, backups, logs, local Postgres data
tmp/

# Nightly app-schema dumps (backup_app.py) — local safety net, not source
backups/

# Python
.venv/
__pycache__/
*.pyc

# Frontend — independently version-controlled (its own git repo + GitHub remote).
# The parent fab repo does not track it (see CLAUDE.md).
retro-data-display/

# dbt artifacts
fab_dbt/target/
fab_dbt/logs/
fab_dbt/dbt_packages/

# Android scanner build/editor artifacts
fab-scanner-android/.gradle/
fab-scanner-android/.idea/
fab-scanner-android/**/build/
fab-scanner-android/local.properties

# Stray logs
*.log
